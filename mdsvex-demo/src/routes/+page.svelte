<h1>mdsvex Demo</h1>

Visit /page1 and /page2 routes.

<p>
  `mdsvex` is useful when you want to have markdown in your local codebase
  files.
</p>

<p>
  <u>
    Note from Sahil: If you want to render some markdown from a url at runtime
    then you can simply use some other <code>markdown-to-html</code> library instead.
  </u>
</p>
